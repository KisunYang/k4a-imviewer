# RVM 모델

이 폴더에 RVM ONNX(예: `rvm_mobilenetv3_fp32.onnx`)를 두면 앱에서 로드할 수 있습니다.  
용량이 커서 기본 `.gitignore`에서는 ONNX/PyTorch 가중치를 제외합니다.  
GitHub에 포함하려면 해당 규칙을 지우거나 [Git LFS](https://git-lfs.github.com/)를 사용하세요.
